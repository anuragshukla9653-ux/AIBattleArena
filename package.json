{
  "name": "ai-battle-arena",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --production=false --prefix Backend && npm install --production=false --prefix Frontend && npm run build --prefix Frontend && npm run build --prefix Backend",
    "start": "npm start --prefix Backend",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix Frontend",
    "build": "npm run build --prefix Frontend && npm run build --prefix Backend"
  },
  "engines": {
    "node": ">=20"
  }
}
